# Security

Do not include API keys, passwords, tokens, VPN credentials, media paths
containing private information, or other secrets in issues or pull requests.

Media Optimizer currently uses API credentials supplied through environment
variables.

The project is currently alpha software and write functionality is intentionally
disabled while safety behavior is being validated.
